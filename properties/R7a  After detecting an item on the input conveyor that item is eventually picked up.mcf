[true* . itemDetected(input)] nu Y.
(
  mu X.
  (
    [!(
      pickItem(input) ||
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(reset) || stopBelt(reset) ||
      alarm
    )] X
    &&
    [
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(reset) || stopBelt(reset) ||
      alarm
    ] Y
    &&
    <true>true
  )
)