[true* . itemDetected(reset)] nu Y.
(
  mu X.
  (
    [!(
      pickItem(reset) ||
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(input) || stopBelt(input) ||
      alarm
    )] X
    &&
    [
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(input) || stopBelt(input) ||
      alarm
    ] Y
    &&
    <true>true
  )
)