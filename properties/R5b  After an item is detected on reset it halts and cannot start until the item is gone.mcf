[true* . itemDetected(reset)] nu Y.
(
  mu X.
  (
    [!(
      stopBelt(reset) ||
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(input) || stopBelt(input) ||
      alarm
    )] X
    &&
    [
      startBelt(upgrade) || stopBelt(upgrade) ||
      startBelt(input) || stopBelt(input) ||
      alarm
    ] Y
    &&
    <true>true
  )
)
&&
[
  true* .
  itemDetected(reset) .
  (!pickItem(reset))* .
  startBelt(reset)
] false