[true* . itemDetected(upgrade)] nu Y.
(
  mu X.
  (
    [!(
      stopBelt(upgrade) ||
      startBelt(input) || stopBelt(input) ||
      startBelt(reset) || stopBelt(reset) ||
      alarm
    )] X
    &&
    [
      startBelt(input) || stopBelt(input) ||
      startBelt(reset) || stopBelt(reset) ||
      alarm
    ] Y
    &&
    <true>true
  )
)
&&
[
  true* .
  itemDetected(upgrade) .
  (!pickItem(upgrade))* .
  startBelt(upgrade)
] false