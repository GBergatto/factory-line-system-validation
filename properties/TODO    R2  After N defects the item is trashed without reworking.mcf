nu X(n: Nat = 0).(
  [pickItem(input)]X(0) &&
  [moveToReset]X(n + 1) &&
  [!(
    pickItem(input) ||
    moveToReset
  )]X(n)
  
  &&
  
  (
    val (n == 1) => mu Y.(
      [!moveToTrash]Y &&
      <true>true &&
      [itemDetected(upgrade)]false
    )
  )
)